{
  "name": "cix",
  "version": "0.0.1",
  "description": "An Continuous Integration and Remote Execution Tool",
  "private": true,
  "bin": {
    "cix": "bin/cix"
  },
  "scripts": {
    "format": "prettier --write **/*.{js,json} && standard --fix",
    "lint": "standard",
    "test": "jest",
    "watch": "jest --watch",
    "snapupdate": "jest --updateSnapshot",
    "coverage": "jest --coverage"
  },
  "files": [
    "src",
    "LICENSE",
    "readme.md",
    "docs",
    "bin"
  ],
  "license": "MIT",
  "dependencies": {
    "assign-deep": "^1.0.1",
    "gluegun": "^4.0.1",
    "jwt-simple": "^0.5.6",
    "socket.io-client": "^2.3.0"
  },
  "devDependencies": {
    "jest": "^24.9.0",
    "prettier": "^1.18.2",
    "standard": "^14.3.1"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "standard": {
    "env": [
      "jest"
    ]
  },
  "prettier": {
    "semi": false,
    "singleQuote": true
  }
}
